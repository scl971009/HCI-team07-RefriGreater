<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:dialogTheme">@style/AppTheme.DialogTheme</item>
    </style>
    <style name="SpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:background">@drawable/bg_spinner</item>
    </style>
    <style name="AppTheme.DialogTheme" parent="android:Theme.Material.Light.Dialog">
        <item name="android:colorPrimary">@color/datepicker</item>
        <item name="android:colorPrimaryDark">@color/datepicker</item>
        <item name="android:colorAccent">@color/datepicker</item>
    </style>
    <style name="addSpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:background">@drawable/addbg_spinner</item>
    </style>
    <style name="MONO">
        @import url('https://fonts.googleapis.com/css?family=Roboto+Mono');
    </style>

</resources>
